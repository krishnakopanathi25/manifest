trigger:
- main

pool:
  name: flm-pool

steps:
- script: echo Hello world
  displayName: 'stage-1'

- script: 
    echo Add other tasks to build test and deploy
    echo see https://aka.ms/yaml
  displayName: 'stage-2'
- script: echo "welcome to azure"
  displayName: 'stage-3'
